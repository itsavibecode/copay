# Copay — moved

This project now lives at **https://dev.rizzo.cc/copay/**

This repository is archived and no longer updated, and GitHub Pages here is
disabled. Note that the app stores data per-site in your browser: anything
entered at the old address stays in that browser profile, so import a JSON
backup (or re-enter your plans) at the new address.
